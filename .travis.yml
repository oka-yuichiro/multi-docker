sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t okay/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true okay/react-test npm test

after_success:
  - docker build -t okay/multi-client ./client
  - docker build -t okay/multi-nginx ./nginx
  - docker build -t okay/multi-server ./server
  - docker build -t okay/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push okay/multi-client
  - docker push okay/multi-nginx
  - docker push okay/multi-server
  - docker push okay/multi-worker

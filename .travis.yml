sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yuichiro907/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true yuichiro907/react-test npm test

after_success:
  - docker build -t yuichiro907/multi-client ./client
  - docker build -t yuichiro907/multi-nginx ./nginx
  - docker build -t yuichiro907/multi-server ./server
  - docker build -t yuichiro907/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yuichiro907/multi-client
  - docker push yuichiro907/multi-nginx
  - docker push yuichiro907/multi-server
  - docker push yuichiro907/multi-worker
